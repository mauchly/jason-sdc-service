config:
  target: 'http://localhost:3001'
  phases:
    - duration: 10
      arrivalRate: 20
  defaults:
    headers:
      x-my-service-auth: '987401838271002188298567'
scenarios:
  - flow:
    - get:
        url: "/getBookedDates"
        qs:
          listingId: 9500000

